name: Variable Flow Demo
on:

jobs:
  Explore-Variable-Flow:
    runs-on: ubuntu-latest
    environment: "staging"
    steps:
      - name: Check the env/var and secret value
        run: |
          echo '${{ vars.STATUS }}'
          echo '${{ secrets.PASSWORD }}' 

